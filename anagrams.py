
# take a word
# rearrange it and check if in dictionary
# return new word

def makeAnagram(word, d):
    return

def run:
    words = int(sys.argv[1])
    file = open('/usr/share/dict/words','r')
    content = list(file)

    gameState = input('Do you want to play? (y/n): ')
    while (gameState == 'Y' or gameState == 'y'):
        #do stuff
        return

    return